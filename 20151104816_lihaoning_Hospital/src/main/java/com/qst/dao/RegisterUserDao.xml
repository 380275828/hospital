<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qst.dao.RegisterUserDao">

		<insert id="RegisterByBean"
		parameterType="User">
		
		insert into userlogin(User_Id,User_Name,User_Iphone,User_Password)
		values(NULL,#{User_Name},#{User_Iphone},#{User_Password})
		</insert>
		
		<insert id="updateByBean"
		parameterType="User">
		update elem_user set user_name=#{user_name},user_address=#{user_address},user_phone=#{user_phone}
		where user_id=#{user_id}
		</insert>
</mapper>